/**
 * FileUpload component - Handles file uploads (text, images, PDFs).
 */

function FileUpload() {
  return (
    <div>
      {/* TODO: Implement file upload with drag-and-drop support */}
      <p>FileUpload placeholder</p>
    </div>
  );
}

export default FileUpload;
