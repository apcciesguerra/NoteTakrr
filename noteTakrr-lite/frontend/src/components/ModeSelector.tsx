/**
 * ModeSelector component - Toggle between 'summary' and 'reviewer' modes.
 */

function ModeSelector() {
  return (
    <div>
      {/* TODO: Implement mode toggle (summary / reviewer) */}
      <p>ModeSelector placeholder</p>
    </div>
  );
}

export default ModeSelector;
